server:
  port: 9000

# 数据源配置
spring:
  datasource:
    username: scnua204
    password: scnua204
    url: jdbc:mysql://115.29.201.93:3306/er?useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

# mybatis配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

# fastdep-shiro-jwt配置
fastdep:
  shiro-jwt:
    filter:
      front:
        path: /front/**
        role: anon
      api:
        path: /api/v1/**
        role: jwt
    secret: 6Dx8SIuaHXJYnpsG18SSpjPs50lZcT52
    expireTime: 7200000 # token有效期
    prefix: ""  # token校验时的前缀
    signPrefix: "" # token生成签名的前缀
    header: "Authorization" # token校验时的header头

# 日志配置
logging:
  level:
    com.qraffa.easyrentboot.dao: DEBUG